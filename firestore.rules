rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: Lock these down to authenticated users before going live.
    // For now, open read/write for local development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
